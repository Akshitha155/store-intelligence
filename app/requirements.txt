# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-multipart==0.0.12

# Database
# (SQLite is built-in, no extra package needed)

# HTTP client (used by pipeline to post events)
requests==2.32.3

# Testing
pytest==8.3.2
pytest-cov==5.0.0
httpx==0.27.2  # required by FastAPI TestClient
